{
  "name": "configuration-app",
  "version": "0.5.1",
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "glob": "^7.1.3",
    "lodash": "^4.17.11"
  },
  "scripts": {
    "start": "node scripts/start.js",
    "back": "nodemon server/server.js",
    "build": "node scripts/build.js && mv build server",
    "prod": "nodemon server/server.js",
    "test": "node scripts/test.js --env=jsdom"
  },
  "devDependencies": {},
  "babel": {
    "presets": [
      "react-app"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "proxy": "http://localhost:3001"
}
